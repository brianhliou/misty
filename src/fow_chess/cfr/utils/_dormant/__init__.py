"""Dormant utilities — code no active component imports.

Lives here so it's discoverable but not implicitly pulled in. Nothing
in `_dormant` is exported by the parent package. If you find yourself
reaching for something here, that's a signal — either promote it out
of `_dormant` (and write the consumer), or accept that it stays
dormant.

Parked here during Phase A0 of the Obscuro replication
(`docs/engine-track/phase-a-scope-2026-05-22.md`).
"""
